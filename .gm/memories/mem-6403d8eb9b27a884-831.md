---
key: mem-6403d8eb9b27a884-831
ns: default
created: 1782672278093
updated: 1782672278093
---

gm-method: prd-resolve and prd-add both match the id field FUZZILY not exactly. Dispatching three prd-resolve with ids chain-fsm-maturity/model-resolver-coverage/named-chains-builtin (rows already resolved by a prior session, absent from pending) fuzzy-landed on the three nearest PENDING rows perf-chain-latency/perf-format-translation/perf-key-rotation and wrongly closed them. ALWAYS read the {resolved:<id>} response and confirm equality with intent; on divergence, immediately re-open the mis-closed row via prd-add with its EXACT existing id+original scope (returns {rescoped:<id>}, restores to pending preserving handle). Corollary: before resolving, verify the target id is actually in the current pending/ready_wave set — if it is not listed, it was already resolved and any resolve will fuzzy-hit an unrelated live row.
