---
key: mem-78a3b8cb5403c8ab-1181
ns: default
created: 1788188122622
updated: 1788188122622
---

acptoapi chain fluidity: four compounding gaps caused multi-minute hangs on /v1/chat/completions even though each individual timeout/retry budget was reasonable - (1) availability.js latencyPenalty capped far lower than successBonus, letting a 50s model outrank a 2s model; (2) model_unhealthy preCheck waited the full same-link budget with no ETA, unlike sampler_backoff which has a real nextRetryAt; (3) availability.recordFailure only fires on same-link-retry-loop exhaustion, not per attempt, so concurrent requests had zero visibility into a shared model already storming; (4) no overall chain wall-clock ceiling summed per-link budgets across a 9-12 link chain. Also: two env vars (ACPTOAPI_LINK_TIMEOUT_MS vs ACPTOAPI_CHAIN_LINK_TIMEOUT_MS) silently only one of which actually reached real /v1/chat/completions traffic - always check server.js's handleChat directly for which var a route actually consumes, don't trust the more-obviously-named one. And: /v1/chat/completions streaming was fake (buffered full response, synthesized one SSE chunk) despite real incremental streaming machinery (sdk.stream -> providers/openai.js streamOpenAI) already existing elsewhere unused.
